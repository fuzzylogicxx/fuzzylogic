---
layout: single_column_layout.liquid
isHome: true
bodyClass: page-home
title: "Welcome to Fuzzy Logic: Laurence Hughes‘s online home."
metaDescription: Fuzzy Logic is the personal website of Laurence Hughes, a web developer based in Glasgow, Scotland.
permalink: index.html
---

<div class="container">

  <section class="intro prose">

    <h1>Fuzzy Logic</h1>

    <p class="no-hyphens">Hi – I’m Laurence Hughes, a Glaswegian web developer, maker of music and pinger of pongs. This is my online home, where I try out web stuff and write about code, design, music and more.</p>

  </section>

</div>

<div class="centre-with-gutter">

  <section class="home-feed-wrap h-feed centre">

    {% for post in collections.feed limit:5 %}
      <article class="h-entry prose">
        <header>
          <time class="dt-published">{{ post.date | date: "%A, %b %d, %Y" }}</time>
          {%- if post.data.pageTitle -%}
          <h3 class="feed-entry-title">
            {%- if post.data.linkTarget %}
            <a href="{{ post.data.linkTarget | url }}" class="text-icon-pair offsite"><span class="p-name">{{ post.data.pageTitle }}</span> <svg class="text-icon-pair-icon" aria-hidden="true" focusable="false" width="1em" height="1em"><use xlink:href="#icon-offsite"></use></svg></a>
            {%- else if -%}
            <a rel="bookmark" class="p-name u-url" href="{{ post.url | url }}">{{ post.data.pageTitle }}</a>
            {%- endif -%}
          </h3>
          {%- endif -%}
        </header>
        <div class="e-content">
          {{ post.templateContent }}
        </div>
        <!-- <footer>more meta stuff</footer> -->
      </article>
    {% endfor %}

  </section>

</div>
