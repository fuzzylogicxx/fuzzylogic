---
title: "Animating the dialog element with CSS’s @starting-style  "
description: Foo
noteWithTitle: false
date:
  ? "{ now }"
---
I’ve written before that CSS `transition` is great for simple animations triggered by an event. 

The CSS at-rule `@starting-style` is used to define starting values for properties set on an element that you want to transition from when the element receives its first style update, i.e., when an element is first displayed on a previously loaded page.
